<UserControl x:Class="ASLInputTool.Controls.UnitFilterControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="400">
    <StackPanel Orientation="Horizontal" Margin="0,0,0,10" Style="{StaticResource FilterStackPanelStyle}">
        <Label Content="Filter by Nationality:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="SemiBold"/>
        <ComboBox Width="150" 
                  ItemsSource="{Binding Nationalities}" 
                  SelectedItem="{Binding SelectedNationalityFilter}"
                  Style="{StaticResource FilterComboBoxStyle}"/>
        <Button Content="Clear" 
                Command="{Binding ClearFilterCommand}"
                Visibility="{Binding SelectedNationalityFilter, Converter={StaticResource NullToVis}, ConverterParameter=Inverse}"
                Margin="10,0,0,0" Padding="10,2"/>
    </StackPanel>
</UserControl>
