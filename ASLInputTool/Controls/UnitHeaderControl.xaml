<UserControl x:Class="ASLInputTool.Controls.UnitHeaderControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="800">
    <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch" Margin="0,0,0,20">
        <TextBlock Text="{Binding Title, RelativeSource={RelativeSource AncestorType=UserControl}}" FontSize="24" FontWeight="Bold" VerticalAlignment="Center"/>
        <Button Content="{Binding AddButtonText, RelativeSource={RelativeSource AncestorType=UserControl}}" Command="{Binding AddCommand}" 
                Visibility="{Binding IsAdding, Converter={StaticResource InverseBoolToVis}}"
                Style="{StaticResource HeaderAddButtonStyle}"/>
        <Button Content="Delete Selected" Command="{Binding DeleteSelectedCommand}"
                Visibility="{Binding HasSelectedItems, Converter={StaticResource BoolToVis}}"
                Style="{StaticResource HeaderDeleteButtonStyle}"/>
        <Button Content="Back to List" Command="{Binding CancelCommand}" 
                Visibility="{Binding IsAdding, Converter={StaticResource BoolToVis}}"
                Style="{StaticResource HeaderBackButtonStyle}"/>
    </StackPanel>
</UserControl>
