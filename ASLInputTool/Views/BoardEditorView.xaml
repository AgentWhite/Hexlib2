<UserControl x:Class="ASLInputTool.Views.BoardEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="{StaticResource WindowBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Border Grid.Row="0" Background="{StaticResource ControlBorderBrush}" Padding="10">
            <StackPanel Orientation="Horizontal">
                <Button Content="Zoom In" Margin="0,0,10,0" Padding="10,5"/>
                <Button Content="Zoom Out" Margin="0,0,10,0" Padding="10,5"/>
                <Separator Margin="10,0"/>
                <Button Content="Select" Margin="0,0,10,0" Padding="10,5"/>
                <Button Content="Draw" Margin="0,0,10,0" Padding="10,5"/>
            </StackPanel>
        </Border>

        <!-- Canvas Area -->
        <ScrollViewer Grid.Row="1" Background="{StaticResource WindowBackgroundBrush}" 
                      HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <Grid HorizontalAlignment="Center" VerticalAlignment="Center" Margin="50">
                <Border CornerRadius="2">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="15" Direction="-45" RenderingBias="Quality" ShadowDepth="5" Opacity="0.3"/>
                    </Border.Effect>
                    <Grid Width="{Binding ActualGridWidth}" Height="{Binding ActualGridHeight}">
                        <!-- The Canvas for SVG and Bitmap graphics -->
                        <ItemsControl ItemsSource="{Binding Hexes}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <Canvas x:Name="EditorCanvas" Width="{Binding ActualGridWidth}" Height="{Binding ActualGridHeight}" Background="White" ClipToBounds="True" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Canvas>
                                        <Path Data="{Binding Points}" Stroke="Black" StrokeThickness="1" Fill="Transparent">
                                            <Path.ToolTip>
                                                <ToolTip>
                                                    <StackPanel>
                                                        <TextBlock Text="{Binding Column, StringFormat='Col: {0}'}"/>
                                                        <TextBlock Text="{Binding Row, StringFormat='Row: {0}'}"/>
                                                        <TextBlock Text="{Binding Id, StringFormat='ID: {0}'}"/>
                                                    </StackPanel>
                                                </ToolTip>
                                            </Path.ToolTip>
                                        </Path>
                                        <TextBlock Text="{Binding Id}" 
                                                   Canvas.Left="{Binding LabelLeft}" 
                                                   Canvas.Top="{Binding LabelY}"
                                                   Width="{Binding HexSize}"
                                                   TextAlignment="Center"
                                                   FontSize="10"
                                                   Foreground="DarkGray"
                                                   IsHitTestVisible="False" />
                                    </Canvas>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Grid>
                </Border>
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>
