namespace ASLInputTool.ViewModels;

/// <summary>
/// Specifies the type of road being painted with the road tool.
/// </summary>
public enum RoadToolType
{
    /// <summary>Paved road (Gray/Black).</summary>
    Paved,
    /// <summary>Dirt road (Brown).</summary>
    Dirt,
    /// <summary>Path (Thin Brown Dash).</summary>
    Path
}
